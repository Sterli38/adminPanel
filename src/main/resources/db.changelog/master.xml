<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                   https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">
    <changeSet id="create-table_race" author="Leonid Velikanov">
        <preConditions>
            <not>
                <tableExists tableName="race"/>
            </not>
        </preConditions>
        <sql>
            CREATE TABLE race
            (
                id   SERIAL PRIMARY KEY,
                name VARCHAR NOT NULL
            );
        </sql>
    </changeSet>

<!--        TODO объединить с созданием таблицы-->
    <changeSet id="add-init-data-in-table-race" author="Leonid Velikanov">
        <preConditions>
            <sqlCheck expectedResult="0">
                SELECT COUNT(*) FROM race

            </sqlCheck>
        </preConditions>
        <sql>
            INSERT INTO race(name)
            values ('HUMAN'),
                   ('DWARF'),
                   ('ELF'),
                   ('GIANT'),
                   ('ORC'),
                   ('TROLL'),
                   ('HOBBIT');
        </sql>
    </changeSet>

    <changeSet id="create-table-profession" author="Leonid Velikanov">
        <preConditions>
            <not>
                <tableExists tableName="profession"/>
            </not>
        </preConditions>
        <sql>
            CREATE TABLE profession
            (
                id   SERIAL PRIMARY KEY,
                name VARCHAR NOT NULL
            );
        </sql>
    </changeSet>

    <!--    TODO объединить с созданием таблицы-->
    <changeSet id="add-init-data-in-table-profession" author="Leonid Velikanov">
        <preConditions>
            <sqlCheck expectedResult="0">
                SELECT COUNT(*) FROM profession
            </sqlCheck>
        </preConditions>
        <sql>
            INSERT INTO profession(name)
            values ('WARRIOR'),
                   ('ROGUE'),
                   ('SORCERER'),
                   ('CLERIC'),
                   ('PALADIN'),
                   ('NAZGUL'),
                   ('WARLOCK'),
                   ('DRUID');
        </sql>
    </changeSet>

    <changeSet id="create-table-player" author="Leonid Velikanov">
        <preConditions>
            <not>
                <tableExists tableName="player"/>
            </not>
        </preConditions>
        <sql>
            CREATE TABLE player
            (
                id             SERIAL PRIMARY KEY,
                name           VARCHAR(12) NOT NULL UNIQUE,
                title          VARCHAR(30) NOT NULL,
                race_id        INT REFERENCES race (id),
                profession_id  INT REFERENCES profession (id),
                experience     INT         NOT NULL,
                level          INT         NOT NULL,
                untilNextLevel INT         NOT NULL,
                birthday       DATE        NOT NULL,
                banned         BOOLEAN     NOT NULL
            );
        </sql>
    </changeSet>

</databaseChangeLog>